import React from 'react';

// ChatList component displays the list of chats on the right section
const ChatList: React.FC = () => {
  return (
    <div className="chat-list">
      {/* Chat items will be rendered here */}
    </div>
  );
};

export default ChatList;